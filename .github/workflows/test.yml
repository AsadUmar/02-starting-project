name: Test
on: Push
jobs:
  test:
    runs-on: Ubuntu-latest
    steps:
      - name: Get Code
        uses: actions/checkout@v3
      - name: Install
        run: npm ci
      - name: test
        run: npm test
